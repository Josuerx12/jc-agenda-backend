MODE=DEV
PORT=3000
JWT_SECRET=troque-por-um-segredo-aleatorio-com-32-ou-mais-caracteres

DB_HOST=localhost
DB_PORT=5435
DB_USER=api
DB_PASSWORD=change-me
DB_NAME=jc_agenda

# Desenvolvimento local: deixe vazio. Produção: obrigatório.
REDIS_HOST=localhost
REDIS_PORT=6390
REDIS_PASSWORD=
REDIS_TLS=false
REDIS_QUEUE_PREFIX=jc-agenda

SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_REQUIRE_TLS=true
SMTP_USER=
SMTP_PASSWORD=
EMAIL_FROM="JC Agenda <no-reply@jcagenda.com.br>"

PLATFORM_URL_PATTERN=http://{slug}.localhost:9000
PASSWORD_RESET_URL_PATTERN=http://{slug}.localhost:9000/reset-password?token={token}
PASSWORD_RESET_EXPIRES_MINUTES=30
PASSWORD_RESET_COOLDOWN_SECONDS=60

EMAIL_OUTBOX_INTERVAL_MS=3000
EMAIL_OUTBOX_BATCH_SIZE=100
EMAIL_QUEUE_MAX_ATTEMPTS=8
EMAIL_QUEUE_CONCURRENCY=5

CORS_ORIGINS=http://localhost:3000
RATE_LIMIT_TTL_MS=60000
RATE_LIMIT_REQUESTS=100
ENABLE_SWAGGER=true
